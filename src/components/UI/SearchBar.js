import { Fragment, useRef } from 'react';
import classes from './SearchBar.module.css';

function SearchBar(){
    return(
        <Fragment>
            
        </Fragment>
    );
};

export default SearchBar;